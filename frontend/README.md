# Nexus AI Frontend

Polished Angular 19 frontend for the Gemini chatbot API.

## Setup

```bash
npm install
npm start
```

The app runs on `http://localhost:4200` and expects the backend on
`http://localhost:5000`.
